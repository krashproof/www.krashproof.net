machine:
  java:
    version: oraclejdk8
dependencies:
  pre:
    - sudo pip install html5validator
test:
  override:
    - html5validator --root html/
deployment:
  production:
    branch: master
    commands:
      - sudo pip install awscli
      - /bin/bash deploy.sh
