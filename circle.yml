machine:
  java:
    version: oraclejdk8
dependencies:
  pre:
    - sudo pip install html5validator
test:
  override:
    - html5validator --root html/
deployment:
  aws:
    branch: master
    commands:
    - /bin/bash/ deploy.sh
